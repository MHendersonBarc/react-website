import React, { useState } from 'react';
import '../../public/bio.css'; // Import the CSS for styling

const Bio = () => {
    return (
        <div>
            This is the Bio page!
        </div>
    );
};

export default Bio;