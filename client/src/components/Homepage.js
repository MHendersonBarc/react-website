import React, { useState } from 'react';
import '../../public/homepage.css'; // Import the CSS for styling

const Homepage = () => {
    return (
        <div id = "Text">
            This is the Homepage!
        </div>
    );
};

export default Homepage;