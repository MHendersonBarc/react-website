import React, { useState } from 'react';
import '../../public/portfolio.css'; // Import the CSS for styling

const Portfolio = () => {
    return (
        <div>
            This is the Portfolio page!
        </div>
    );
};

export default Portfolio;