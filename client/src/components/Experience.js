import React, { useState } from 'react';
import '../../public/experience.css'; // Import the CSS for styling

const Experience = () => {
    return (
        <div>
            This is the Experience page!
        </div>
    );
};

export default Experience;